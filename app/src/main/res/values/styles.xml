<resources>

    <!--作为中转，在替换CompatAppTheme的parent的时候，只需要在这里做修改-->
    <style name="AppBaseTheme" parent="AppThemeLight">

    </style>

    <style name="CompatAppTheme" parent="AppBaseTheme">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowActionModeOverlay">true</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="CompatAppTheme">
        <!-- Customize your theme here. -->
       <!-- <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>-->
    </style>

    <style name="SplashTheme" parent="CompatAppTheme">
        <!-- 欢迎页背景引用-->
        <item name="android:windowBackground">@drawable/splash</item>
    </style>


</resources>
